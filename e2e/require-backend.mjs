/**
 * バックエンドAPIが起動していないときに、原因が分かる形でテストを止めるためのスクリプト。
 *
 * Playwright の `webServer` は `reuseExistingServer: true` のとき、
 * まず `url` に応答があるかを見て、応答があればこのコマンドを実行しない。
 * つまりこれが動くのは「バックエンドが起動していない」ときだけで、
 * その場合は案内を出して異常終了し、テスト全体を即座に止める。
 *
 * これが無いと、バックエンド停止が
 * 「/stores/map が pageerror を出す」というフロント側の不具合に見えてしまう。
 *
 * 使い方: `node e2e/require-backend.mjs <APIのベースURL>`
 */
const apiUrl = process.argv[2] ?? '(未指定)'

console.error(
    [
        '',
        '━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━',
        ' バックエンドAPIに接続できません。',
        '',
        `   接続先: ${apiUrl}`,
        '',
        ' このスモークテストは実バックエンドに対して実行します。',
        ' 先にバックエンド（nodedeploytest）と PostgreSQL を起動してください。',
        ' Docker が落ちているとAPIは全て 500 を返すので、DBの起動確認も必要です。',
        '',
        ' バックエンド無しで回したい場合は、スタブAPIに切り替えられます：',
        '',
        '   E2E_USE_MOCK_API=1 npm run test:e2e:dev',
        '',
        '━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━',
        ''
    ].join('\n')
)

process.exit(1)
